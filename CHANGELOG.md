# Changelog

## v1.0.0 (2026-06-24)

初回安定版リリース。WARC/HTML/プレーンテキスト/JSONL を入力として受け取り、
ストリーミング処理でクリーニング・スコアリング・重複除去・フィルタリングを行い、
Parquet/JSONL を出力するデータセット前処理パイプライン CLI（`odc`）。

### 機能

- **入力**: WARC(.warc/.warc.gz)、HTML（単体/ディレクトリ/glob）、プレーンテキスト、JSONL をストリーミング読み込み
- **テキスト抽出**: HTMLタグ除去・ボイラープレート除去（nav/footer/広告枠/サイドバー）、Markdown変換オプション（見出し/リスト/リンク保持設定可）
- **品質スコアリング**:
  - 言語検出（lingua-rs）・言語混合率・文字種比率（ひらがな/カタカナ/漢字/英数字/その他）
  - テキスト品質（重複行率・記号数字異常比率・平均文長/文長分散・残留HTML/URL検出）
  - コンテンツ品質（広告キーワード/正規表現検出・SEOスパムスコア・自然さスコア）
  - Perplexityスコアは `perplexity` cargo feature のスキャフォルドのみ（KenLM未接続、デフォルト無効）
- **重複除去**: 完全一致（blake3ハッシュ）＋近似重複（MinHash+LSH、類似度閾値設定可）、ストリーミングと両立する省メモリ設計
- **フィルタリング**: TOML/YAML設定ファイルによる閾値設定、AND/OR/NOT組み合わせルール、WASMプラグイン（wasmtimeサンドボックス、タイムアウト/メモリ制限付き）
- **出力**: Parquet/JSONL、シャード分割（最大行数指定）、除外レコードの理由付き別出力
- **運用系**: rayonによる並列処理、処理統計レポート（除外理由別件数・スコア分布等）、チェックポイントによる再開対応（JSONL出力時）、ログレベル設定

### 既知の制限

- Perplexityスコアは未接続（KenLMネイティブビルドの確認待ち）
- 数百GB〜TB級入力でのメモリ線形性は未実測
- macOS Intel(x86_64-apple-darwin/macos-13)向けバイナリは配布対象外。GitHub Actionsで
  macos-13ランナーが恒久的に割当されない問題（runner_id:0でqueuedのまま進行しない）が
  確認されたため、release.ymlのビルドマトリクスから一時的に除外（windows/linux/macos-arm64
  の3プラットフォームはCIでグリーン確認済み）

### マイルストーン履歴

- M1 `107a73e`: JSONL/プレーンテキスト入力 → 抽出 → 基本スコアリング → JSONL出力
- M2 `91b12dd`: WARC/HTML入力対応、Markdown変換、Parquet出力
- M3 `7069355`: 重複除去（完全一致 → MinHash/LSH）
- `fe4688e`: GitHub Actions リリースビルド・インストールスクリプト追加
- M4 `af014e0`: 設定ファイルによるフィルタリング（AND/OR/NOT）、統計レポート出力
- M5 `f7696da`: WASMプラグインシステム（wasmtime）
- M6 `baf3edb`: チェックポイント/再開対応、並列処理最適化
- M7+ `928d27c`: 出力シャード分割、コンテンツ品質スコアリング
